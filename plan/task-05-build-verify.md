# Task 05 — Build & Verify

## Checks

1. `swift build` — compiles clean (no errors, no warnings)
2. `swift test` — all existing tests pass
3. Manual: `mung` with no args — menu bar icon appears
4. Manual: `mung add --title "Test" --message "Hello"` — appears in popover
5. Manual: dismiss from UI — alert removed
6. Manual: `mung list` — CLI still works
